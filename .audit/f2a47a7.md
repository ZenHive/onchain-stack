# Post-merge audit: f2a47a7

## Scope and requirements

Reviewed `50963c4^..f2a47a7`: task 5006's four Sugar row structs, shared
positional constructor, ABI drift and fixture tests, discovery registration,
README/CLAUDE/AGENTS/changelog updates, the checksum-test correction, and
roadmap-only task transitions (including task 4069).

Task 5006's structural requirements are met by inspection: fields preserve ABI
order; address conversion is explicit; amounts remain integers; constructors
have specs; Lp discriminator and Position zero-value semantics are documented.
Tests read ABI JSON independently and compare both Token captures. Position
fixtures are empty, explicitly acknowledged in the tests and supplemented by a
non-empty synthetic tuple. Full `mix ci` was not rerun; validation below records
only checks actually performed in this audit.

## Finding and fix

One documentation finding, fixed: `Types.Lp` classified `emissions_cap` with
money amounts despite the package's established rule that it is a relative
share in basis points. Removed it from that list and documented its units and
why consumers must not apply it again to the notified emissions rate. No
runtime behavior changed.

No dead code, debug output, missing discovery registration, or changelog gap
was found in the landed changes. The checksum assertion correction correctly
uses ABI address types rather than binary length. No reviewer rejections were
recorded, so there is no false-rejection feedback to assess.

## Follow-ups

No new discovery task was needed. Cold dependency resolution reported the
already-adjudicated cowlib/gun advisories and Mint 1.9.3 advisories. Existing
pending task **9008** already covers Mint fix availability and dependent
packages; no duplicate task was filed. Roadmap and changelog files were left
unchanged.

## Validation

- Independently compared all four source field lists and address subsets with
  the committed ABI JSON: all match (32/18/6/6 fields). Both alternate Position
  endpoints also have identical components to `positions`.
- `git diff --check`: passed.
- Cold witness: `cd packages/onchain_aerodrome && mix check.dispatch` initially
  stopped because Hex dependencies were absent. Ran `mix deps.get` successfully
  with no lock changes, then reran the same check from the initially unwarmed
  tree: **passed**. Formatting, compilation with warnings as errors, Credo,
  Doctor, duplication, architecture/smell checks, and Sobelow completed.
  Sobelow emitted its expected no-router warning for this non-web package.
- No ExUnit suite was run: `check.dispatch` is static-only and the audit fix is
  documentation-only. Fixture-test coverage above was reviewed, not claimed
  as executed.
