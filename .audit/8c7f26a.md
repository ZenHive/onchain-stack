# Audit 8c7f26a

Post-merge hygiene pass over `fdff1a9^..8c7f26a`. The range contains the
precompiled-NIF adoption and v0.6.0 preparation (Tasks 53/62), the independent
EVM semantics harness and assertion follow-up (Tasks 56/63), the tokio runtime
construction spike (Task 33), and the Rust test/clippy gate (Task 43).

## Reviewed

- Tasks 53/62: `RustlerPrecompiled` options, source-build fallback, target and
  checksum handling, cross-build script, package metadata, README/SKILL docs,
  tests, and changelog entry. The installed 0.9.0 HexDocs confirm the option,
  target, checksum, and force-build behavior used here.
- Tasks 56/63: official Ethereum fixtures, verification ledger, revm
  comparators, negative controls, pinned KEVM/Kontrol runner, and changelog.
- Task 33: runtime helper/test, real-node measurement, integration tripwire,
  written decision, and changelog.
- Task 43: both-crate cargo runner, alias wiring, clippy policy, unit tests,
  AGENTS/CLAUDE toolchain notes, and changelog.
- Roadmap/render/state commits in the range were reviewed but not edited.
- Checked for dead code, stale docs, missing changelog coverage, debug residue,
  naming inconsistencies, shell syntax, and whitespace errors.

## Findings and fixes

| # | Finding | Resolution |
|---|---------|------------|
| 1 | The README and build-script release runbooks tagged before building, while pending Task 62 requires artifacts to be built before the public tag is pushed | **Fixed:** both runbooks now use build → tag/push → upload → checksum → publish |
| 2 | The Unreleased changelog retained an older Task 33 measurement after the evidence file was rerun | **Fixed:** aligned it with the final 1.0 µs, 2.906 s / 4.079 s measurements and 0.000034% maximum share |
| 3 | A successful `verify-semantics.sh` run left its temporary KEVM negative-control log behind | **Fixed:** unlink the log after its expected failure has been validated; failure paths retain it for diagnosis |

No discovery task was filed; all findings were small and fixed inline.

## Reviewer feedback

Task 53's rejection on `run-1787383438157-8ee2346b` was **not false**. It
correctly identified that the original release-backed criteria were impossible
to satisfy: v0.5.1 was already published, no assets/checksums existed, and the
load path deadlocked on 404s. The landed work is sound because the task was
rescoped, version 0.6.0 and checkout source-building were added, and the
credentialed release/publish half was split into pending Task 62.

## Checks

- `bash -n scripts/build-precompiled.sh native/onchain_evm/verify-semantics.sh`
  passed.
- Cold witness `mix deps.get && mix check.dispatch` passed. It fetched the
  locked dependencies, compiled both NIFs from source in the unwarmed tree,
  and completed compile/format, Credo, Doctor, ex_dna, Reach, and Sobelow.
- `git diff --check` passed.
