# Audit ad64a01

Post-merge hygiene pass over `df9fd1a^..ad64a01`. The range contains Task 61's
`solang-parser` → `solar-parse` migration, Task 28/29 native unit tests, two
parser-invariant test pins, and roadmap state for Tasks 28, 29, 32, 44, 53,
and 61.

## Reviewed

- `df9fd1a`: replaced `solang-parser` with `solar-parse` 0.2.0, inverted the
  four-source characterization test, and updated crate/docs rust-version to
  1.95+.
- `16977c1` / `22b8432`: pinned source-order / constructor / mutability
  invariants and the parse-depth + diagnostic-rendering regressions that the
  migration resolved as a side effect.
- `7ee9903`: extracted testable helpers in `onchain_evm` and added transport,
  block-id, error-tag, and state-override unit tests.
- `4bc7cc2`: added `onchain_solidity` unit tests for selectors, nested
  canonicalization, NatSpec distance, and the inverted modern-syntax cases.
- Roadmap-only: `d91b69f`, `b4b724b`, `9aeb933`, `dd38e21`, `295adaa`,
  `e769467`, `8a7e985`, `ad64a01`. Not edited by this audit.
- Checked the landed diff for dead code, debug output, leftover `solang`
  references in code, TODO/FIXME residue, and public-doc consistency. No
  reviewer rejections were recorded for this range.

## Findings and fixes

| # | Finding | Resolution |
|---|---------|------------|
| 1 | Unreleased CHANGELOG still described the Task 59 spike as current, so the next release would not mention the parser swap, 0.8.27+ syntax, diagnostic errors, or the depth bound | **Fixed:** recorded the migration under Changed; kept the spike as the design-record Added item |
| 2 | `PARSER_FRONTEND_SPIKE.md` still claimed the characterization test records `solang-parser` rejections, and its follow-up checklist was unchecked after Task 61 landed | **Fixed:** status banner, past-tense spike wording, checked criteria, updated characterization sentence |
| 3 | README / SKILL.md / `Onchain.Solidity` moduledoc named `solar-parse` but did not say source parsing now accepts Solidity through 0.8.36 | **Fixed:** one-sentence capability notes on each surface |

Left untouched: `ROADMAP.md` architecture still names `solang-parser` (roadmap
files are out of this audit's write-scope). The 0.1.0 CHANGELOG line naming
`solang-parser` is historical and correct for that release. Task 28's
`encode_error_tag` string round-trip is test-extract scaffolding, not a
hygiene defect.

No discovery task was filed.

## Cold check

After `mix deps.get` bootstrapped the intentionally un-warmed worktree,
`mix check.dispatch` **passed**. The cold compile built both NIFs from a
cold Cargo cache; Credo, Doctor, ex_dna, Reach, and Sobelow completed
successfully.

The moduledoc edit also compiled clean under `MIX_ENV=test mix compile
--warnings-as-errors`.
