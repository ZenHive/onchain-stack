# Audit 408b2b6

Reviewed `cccbfc3^..408b2b6`: task 2134 implementation, the cross-chain regression test in `a93e200`, and the roadmap completion commit. Inspected the signing/recovery implementation, specs, README, tests, chain registry, and package changelog.

## Findings and fixes

1. **Fixed — signature-width documentation.** The new docs claimed chain IDs through 110 always fit a single-byte EIP-155 v. At chain ID 110, v is 255 or 256 according to recovery parity. Corrected the Signer docs and the signature type comment. Removed the unsupported 68-byte upper bound and the inaccurate claim that all registered live chains except mainnet use wider signatures (Optimism and BNB also fit one byte).
2. **Fixed — envelope instructions.** README wording suggested applying `prefix_eth/1` when the caller already had the envelope. Clarified that `recover_eth/2` accepts that envelope directly; manual prefixing is only needed to construct it.
3. **Filed — release-note gap, task 9007.** No changelog entry describes task 2134's byte-length fix, new recovery helper, packed signature contract, or explicit chain-id normalization. Left CHANGELOG unchanged under the harness operational rule and filed the release-preparation task.
4. **Filed — dependency warning, task 9008.** Cold dependency resolution reported two public advisories for locked Mint 1.9.3. This is a pre-existing dependency observation, not introduced by the audited commits. Filed investigation/update work without changing or suppressing dependencies in this hygiene pass.

The two follow-ups were created with `rmap new --from-stdin`; their generated roadmap changes are included pursuant to the audit-specific discovery-filing instruction.

No runtime changes were needed. The byte-length implementation, helper prefixing, multi-byte decoding/normalization, specs, and cross-chain regression tests are consistent. No leftover debug output or dead code was introduced. Retaining variable-width EIP-155 signatures is the accepted alternative in task 2134 and preserves the existing legacy transaction representation. No reviewer rejections were supplied, so there is no false-rejection finding.

## Cold-build witness

- Initial `cd packages/cartouche && mix check.dispatch` failed because this intentionally cold worktree had no fetched dependencies.
- Ran `cd packages/cartouche && mix deps.get`; the committed lock remained unchanged.
- Reran `cd packages/cartouche && mix check.dispatch` from the freshly fetched, unbuilt dependency tree: **passed**, exit 0. Compile, formatting, Credo, Doctor, duplication, architecture/smell, Sobelow, and the configured test suite passed.
- Test summary: **1,303 passed, 0 failed, 50 excluded, 0 skipped** (seed 612198). The exclusions are the existing dispatch configuration, unchanged by this audit.
- `git diff --check` passed. Audit edits are documentation/comments only. Full `mix ci`, coverage, and Dialyzer were not run; this witness is the requested dispatch check.

Summary: four findings, two fixed, two filed follow-ups. This is a post-merge fix-forward audit, not a new merge gate.
