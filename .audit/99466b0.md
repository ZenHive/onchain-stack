# Post-merge audit: 99466b0

## Scope

Reviewed `d245a3fb2670` (task 4059 implementation) and `99466b0` (roadmap completion), including Pool and DebtToken APIs, unit and integration test changes, README, changelog, and task acceptance criteria. This is a fix-forward hygiene audit of the settled merge.

## Findings and fixes

One documentation finding, fixed: the new `Pool.get_reserve_variable_debt_token/2` and bang variant advertised `:block` without the archive-node caveat required by the package's Node Portability instructions. Their generated API documentation now states that historical reads require an archive node and may receive `-32001` from a pruned or plan-limited endpoint.

The landed change removes the obsolete stable-rate helpers and positional reserve tuple decoding, consistently rejects stable mode locally, and documents the breaking change in README and CHANGELOG. Tests cover variable getter routing, checksummed output, RPC errors, local rejection, bang errors, and independent pinned-fork evidence. No leftover debug output or additional actionable hygiene findings were identified. No deferred discoveries or rmap tasks were filed. No reviewer rejections were recorded for this range.

## Verification

- Cold command: `cd packages/onchain_aave && mix check.dispatch`.
- The initial attempt stopped because dependencies were absent in the intentionally cold worktree. Ran `mix deps.get`, then reran the same check successfully (exit 0), including the documentation fix.
- The dispatch alias runs formatting, compilation with warnings as errors, Credo, Doctor, clone detection, Reach architecture/smells, and Sobelow. Reach reported no smell issues; Sobelow completed with its no-router warning for this library.
- `git diff --check` passed.
- No unit or live integration tests were rerun: the audit changes only generated API documentation, and the dispatch alias contains no test step. Existing pinned-fork evidence was inspected, not independently reproduced here.

Roadmap and changelog files were left unchanged by this audit.
