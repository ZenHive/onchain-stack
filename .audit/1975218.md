# Audit — 1975218 (range b7aba76..1975218)

Date: 2026-09-21
Integrated revision: `1975218aa30d6b4a6acbccb03d1063a7d65f63d1`
Scope: 5 commits already on `main`. Hygiene plus required full-project `mix ci`.

| Commit | Subject |
|---|---|
| `1975218` | roadmap: task 9022 -> done (shipped 5b38a852ad43) |
| `5b38a85` | harness: agent delivery — task 9022 Separate focused dispatch aliases from complete post-merge QA |
| `3b5080e` | roadmap: task 9022 -> in_progress |
| `84c1cbe` | roadmap: complete focused dispatch and audit QA separation |
| `31b9e19` | chore: remove uninstalled ZenHive plugin references |

No reviewer rejections are recorded for this project, so there is no false-rejection loop to assess. Task 9022's landed work matches its acceptance criteria: every package `check.dispatch` is format + compile only, `ci` still expands `precommit.full` (not `check.dispatch`), the root dispatch guard still Mix.raises, and `elixir test/alias_separation_test.exs` proves the before/after graph (17 passed in this audit).

Prior cold `mix ci` attempts on `31b9e19` were incomplete (interrupted at the prompt). This audit ran the gate to a result.

## What was reviewed

- `5b38a85`: all eight package `mix.exs` aliases, root `mix.exs` (unchanged), `test/alias_graph.exs`, `test/alias_separation_test.exs`, `test/fixtures/aliases_before.exs`, root and package `CLAUDE.md` / `AGENTS.md`.
- `31b9e19`: `.claude/settings.json` (removed `elixir@zenhive`, `elixir-workflows@zenhive`, `harness@zenhive`, `dep-audit@zenhive`; kept `extraKnownMarketplaces.zenhive` and `defi-skills@nethermind-defi-skills`).
- Roadmap-only commits `84c1cbe` / `3b5080e` / `1975218` for 9022 status (no code).
- Grep over the landed mix/test/docs surface for leftover debug, `TODO`/`FIXME`, and dispatch/QA wording drift.

Judged not worth changing:

- CHANGELOG entries in the eight Hex packages for Mix-alias operator tooling.
- Leftover `extraKnownMarketplaces.zenhive` after 31b9e19 — it is a marketplace registration, not an enabled plugin; re-enable stays possible.
- Task 9011's historical "add tests to dispatch" text. 9022 reversed that on purpose and is `done`.
- Root `mix.exs` compiling the eight `only: :dev` path deps under default `:dev` before `onchain.bounds`. Wasteful, not a red.

## Findings

### 1. Cartouche CLAUDE.md still claimed a private `.credo.exs` (fixed)

`packages/cartouche/CLAUDE.md` (touched in 5b38a85, toolchain section) said the package "carries its own `.credo.exs`" and pointed at a root open item that no longer exists. All eight `packages/<name>/.credo.exs` are symlinks to the root file; root `CLAUDE.md` already states that.

**Fixed:** rewrote the bullet to match the symlink layout. Regenerated `packages/cartouche/AGENTS.md` with `/data/postgresql/code/claude-marketplace/scripts/sync-agents-md.sh` (the path `agents.check` looks up, `~/_DATA/code/claude-marketplace/scripts/sync-agents-md.sh`, is absent on this host). `--check` passed. The regen also inlined current host `@`-import drift (harness graceful-shutdown section); that is generator output, not a hand edit.

### 2. Root `mix ci` dies 139 in onchain_aave / onchain_aerodrome after green gates (filed task 9023)

Not a 9022 alias bug. After every aave/aerodrome check printed green, the Mix VM exited SIGSEGV (139). Root `packages_ci/1` therefore Mix.raises. Independent rerun of aerodrome reproduced it. `onchain_evm`'s own `mix ci` returned 0. See QA below. Substantial NIF/halt work is out of this audit; filed **task 9023**.

### 3. Alias-separation tests are not in root `mix ci` (filed task 9024)

`elixir test/alias_separation_test.exs` is documented as the 9022 regression check and passed here (17 tests). Root `ci` is still only `onchain.bounds` + `packages_ci/1`. Drift in the alias graph would not fail the family gate. Wiring it is a small Mix/fixture change; not done in this audit. Filed **task 9024**.

## Filed tasks

- **9023** — `mix ci` in onchain_aave and onchain_aerodrome exits 139 after every gate is already green (`assignee = "cursor"`, `model = "cursor-grok-4.6-high"`).
- **9024** — Wire `elixir test/alias_separation_test.exs` into the root `mix ci` alias (same routing).

## QA — `mix ci` at 1975218aa30d6b4a6acbccb03d1063a7d65f63d1

**status: failed**

Command: `mix ci` (exact). Cold tree: first invocation exited 1 in ~3s (`Unchecked dependencies for environment dev`). `mix deps.get` at root and in each `packages/<name>` (locks unchanged), then `mix ci` again.

Root `mix ci`:

1. Compiled root path deps under `:dev`, then `onchain.bounds`: 10 sibling requirements OK.
2. `[1/8] packages/hieroglyph` — mix ci completed. Tests 531 passed / 0 failed; coverage 100% (threshold 95); doctor passed; advisory-freshness OK at 5246bcc; deps.audit no vulnerabilities; reach arch OK; sobelow scan complete; dialyzer JSON 0 warnings.
3. `[2/8] packages/cartouche` — mix ci completed. Tests 1322 passed / 50 excluded / 0 failed; coverage 94.39% (threshold 85); doctor, audit, dialyzer green.
4. `[3/8] packages/onchain` — mix ci completed. Tests 885 passed / 193 excluded / 0 failed; coverage 79.51% (threshold 70); doctor, audit, dialyzer green.
5. `[4/8] packages/onchain_aave` — configured checks green, then process 139. Tests 406 passed / 81 excluded / 0 failed; coverage 100% (threshold 65); doctor; advisory-freshness OK; deps.audit no vulnerabilities; dialyzer 0 errors "done (passed successfully)"; agents.check skipped (host script missing). Mix.raise: `mix ci failed in packages/onchain_aave (exited 139)`. Root log EXIT:139 at 2026-09-21T00:32:44Z.

Independent serial continuation (same `env -u MIX_ENV -u MIX_TARGET mix ci`, never parallel):

| Package | mix ci exit | Notes |
|---|---|---|
| onchain_aerodrome | 139 | Same shape as aave: 172 passed / 2 excluded, coverage 82.25% vs 65, doctor/audit/dialyzer green, agents.check skip, then 139 |
| onchain_evm | 0 | 298 passed / 45 excluded; Rust 28 + 7 ok; coverage 94.73% vs 85; dialyzer 7 skipped; coverage instrumentation warned on EVM/Solidity beams |
| onchain_js | 0 | 14 passed / 6 excluded; coverage 35% vs 25; dialyzer 0 |
| onchain_tempo | 0 | 163 passed / 8 excluded; coverage 97.12% vs 90; dialyzer 0 (1 unnecessary skip) |

Also ran `elixir test/alias_separation_test.exs`: 17 passed (not part of `mix ci`).

agents.check skipped in every package: `~/_DATA/code/claude-marketplace/scripts/sync-agents-md.sh` is not on this host. The generator does exist at `/data/postgresql/code/claude-marketplace/scripts/sync-agents-md.sh`. Designed skip, not a silent green. advisory-freshness ran (task 9003).

Mint 1.10.0 `EEF-CVE-2026-82672` appeared on root `mix deps.get`. Distinct from pending **9012** (aave lock mint 1.9.3 / 82728+82729). gun/cowlib findings are the adjudicated false positive. No extra mint task filed.

Repair for the red: task **9023** (semantically new; no existing 139/SIGSEGV task). Not implemented here.

## Outcome

1 hygiene fix (cartouche credo docs + AGENTS regen). 2 tasks filed (9023, 9024). Family `mix ci` failed on consumer 139 after green aave/aerodrome gates; six of eight package `mix ci` runs returned 0.
