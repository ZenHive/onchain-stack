# Audit b3d38d4

Post-merge hygiene pass over `9020b30^..b3d38d4`. The range contains Task 60's
fork-aware EVM revision selection, the stale Dialyzer-ignore cleanup, and
roadmap state for Tasks 53, 56, 60, and 61.

## Reviewed

- `9a3dd66`: removed five stale transitive-dependency Dialyzer ignores and
  documented the two warning patterns that remain live.
- `7e0c5ab`: added the mainnet hardfork schedule, propagated the selected
  `SpecId` through all three simulation paths, rejected unknown chain ids, and
  added Rust boundary tests plus an archive-node Cancun integration test.
- `9020b30`, `4c8366f`, `03c93ec`, `48456f0`, and `b3d38d4`: roadmap-only
  planning and lifecycle commits; no roadmap files were changed by this audit.
- Checked the landed diff for dead code, debug output, TODO/FIXME residue,
  whitespace errors, stale limitation comments, naming drift, and public-doc
  consistency. No reviewer rejections were recorded for this range.

## Findings and fixes

| # | Finding | Resolution |
|---|---------|------------|
| 1 | Task 60's user-visible fork-revision behavior was absent from the Unreleased CHANGELOG | **Fixed:** documented mainnet `SpecId` selection, unknown-chain rejection, and the Cancun boundary test |
| 2 | `Onchain.EVM` still claimed it could fork through "any RPC endpoint," conflicting with the new mainnet-only chain-id contract | **Fixed:** narrowed the moduledoc to an Ethereum mainnet RPC endpoint |
| 3 | Bare `mix deps.audit` ignored the tracked advisory baseline, so the host hook re-reported the documented `gun 2.5.0` upstream range-merging false positive | **Fixed:** made `deps.audit` apply `.mix_audit_ignore` by default and routed `deps.audit.gated` through it |

No source-code hygiene defect or deferred follow-up remained after these fixes.
No discovery task was filed.

## Cold check

After `mix deps.get` bootstrapped the intentionally un-warmed worktree,
`mix check.dispatch` **passed**. The cold compile emitted upstream dependency
warnings, then project compilation, Credo, Doctor, ex_dna, Reach, and Sobelow
all completed successfully.

The final diff also passed bare `mix deps.audit` with the tracked ignore and a
warmed `mix check.dispatch` rerun.
