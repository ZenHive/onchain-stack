# Audit Report: d38e455

## Range Reviewed

- `e2cbf97` — harness delivery for ROADMAP Task 98, tightening `Cartouche.Typed.encode_value_map/3` so Dialyzer infers `binary()` again and adding return-shape regression tests.
- `d38e455` — review cleanup deleting the stray tracked `.sobelow-skips.committed` duplicate.

## Findings

1. Documentation/bookkeeping gap: Task 98 landed, but `ROADMAP.md` still showed Task 98 as pending and the Current Focus text still described it as a future follow-up. `CHANGELOG.md` also had no Task 98 entry under `[Unreleased]`.

No runtime hygiene issues found. The Typed change is intentionally narrow: each branch feeding the binary comprehension is normalized via `IO.iodata_to_binary/1`, preserving the EIP-712 hash paths while allowing the stricter private `@spec`.

## Fixes Applied

- Added a `[Unreleased]` changelog bullet for Task 98 and the `.sobelow-skips.committed` cleanup.
- Updated the ROADMAP Current Focus and Phase 4 task table to mark Task 98 complete and describe the restored `binary()` contract.

No reviewer rejections were recorded for this range.
