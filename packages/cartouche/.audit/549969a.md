# Audit — `549969a` (range `b070a0e..549969a`, 13 commits)

Post-merge hygiene pass over the work that landed since `audit(b070a0e)`. Fix-forward only; nothing reverted or blocked.

## Range reviewed

| Theme | Commits |
|---|---|
| Task 109 — typed-tx helper dedup (`maybe_to_wei`/`chain_id_value` extraction; V3→TypedDecode/Signature) | `3e681e4` |
| Task 110 — `defrpc` DSL + ETH uniform RPC wrapper conversion | `3b8f27b` |
| `mix ci` gate (ex_dna/reach/ex_slop) + surfaced-finding fixes | `97869ab` |
| Stale IERC20 generated fixture regen onto `%Call{}` (73 test-env dialyzer warnings) | `5f4086f` |
| MIX_ENV=test dialyzer-gate + stale-fixture docs (CLAUDE.md / AGENTS.md) | `549969a` |
| Sleuth/Solana spec tightening | `5563a01`, `d3522f5` |
| Roadmap churn (tasks 109–111 add/reframe/status) | `8836758`, `151b56b`, `da4de64`, `409bd42`, `c5faea1`, `9349239` |

## Findings

**1. (fixed) `V3` left literal error strings after introducing `@invalid`.**
`lib/cartouche/transaction/v3.ex` added `@invalid "invalid v3 transaction"` (used by the new `TypedDecode.decode/4` delegation) but its 15 other error sites still carried the bare literal `{:error, "invalid v3 transaction"}`. Sibling modules `V4` and `V_2930` — refactored in the same range — use their own `@invalid` attribute consistently (0 literals each). V3 was the lone outlier. Replaced all 15 literals with `@invalid` (value-identical; pure consistency). Compile clean, V3 + Signature tests green (24/0).

**2. (filed — task 112) `V4.get_signature/1` is residual un-deduped extraction.**
Task 109 extracted the shared signature triple helpers into `Cartouche.Transaction.Signature` and migrated `V3.get_signature/1` onto `Signature.get/1`, but `V4.get_signature/1` still hand-rolls the identical nil-guard error clause and the packed `r <> s <> y_parity` assembly the new module now owns — the residual half of the "uneven extraction" the dedup task set out to finish. Behavior-preserving but a refactor with a subtle encoding-equivalence check (`<<y_parity_integer(v)>>` vs `:binary.encode_unsigned/1`), so filed as cross-session follow-up rather than widened into this audit. **rmap task 112** (cartouche, bundle `phase8_dedup`).

## Reviewed clean (no action)

- **`defrpc` DSL** (`lib/cartouche/rpc/dsl.ex`) — well-documented, two uniform shapes only; non-uniform wrappers (`get_nonce`) deliberately left hand-written. New public helpers `Chain.chain_id_value/1` and `Wei.maybe_to_wei/1` carry `api()` blocks, `@spec`, doctests, and ExUnit coverage.
- **`Signature` extraction** — clean map-based helpers with a dedicated test file; no dead code (every function has a live caller).
- **Bare-rescue tightening** — `_ ->` rescues narrowed to `ArgumentError ->` across V3/V4/V_2930 with explanatory comments; the one intentional catch-all in `RPC.decode_result/4` is annotated and reach-suppressed with rationale.
- **IERC20 fixture regen** (`5f4086f`) — matches the documented `mix cartouche.gen` regeneration procedure; the file-level credo MaxLineLength disable is preserved.
- **CHANGELOG** — not updated in this range. Judged correct: the changes are internal, behavior-preserving refactors (defrpc emits byte-identical `api()` introspection; helper extraction is pure dedup) with no consumer-facing surface change, and the CHANGELOG groups entries under cut version sections (top is `0.5.0`, no `[Unreleased]`). Not a gap.
- **AGENTS.md / CLAUDE.md** kept in sync (both +9 in `549969a`).

## Reviewer rejections

None recorded for this project.

## Cold check

`MIX_ENV=test mix ci` in this un-warmed worktree (cold `deps.get` + cold PLT): **passed** (exit 0; dialyzer 5 errors all in `.dialyzer_ignore.exs`, "done (passed successfully)"). Re-confirmed compile-clean after the finding-1 fix.
