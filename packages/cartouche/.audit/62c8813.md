# Audit — `62c8813` (range `111488b..62c8813`)

Post-merge hygiene pass over the work that landed since `audit(1c8adb1)`. Fix-forward only; nothing reverted or blocked. No rmap tasks filed.

## Range reviewed

| Theme | Commits |
|---|---|
| Task 117 — encode must not emit wire-non-conformant RLP (V2/V3/V4/V_2930) | `657ec49`, `710868e` |
| Task 113 — property + cross-implementation vector suite | `91e84c7`, `6c8acb2`, `d858b48` |
| Task 114 — mutation-adequacy campaign; muex cannot score on Elixir 1.20 | `8acc2b6` |
| `mix check.dispatch` gains `reach.check`; 117 recorded in CHANGELOG | `d7b876b` |
| Releases 0.7.2 then 0.8.0 (V_2930 write surface is new public API) | `9a8bee8`, `2809e6f` |
| Tasks 120/122/123 — `create_access_list/2`, `eth_baseFee`/`eth_blobBaseFee`, `eth_config`/`eth_capabilities` | `670b22b` |
| Roadmap status / filing (tasks 69, 113–114, 117, 119–124) | remaining commits in range |

## Findings

**1. (fixed) Tasks 120/122/123 shipped with no Unreleased CHANGELOG entry.**
`670b22b` added five public RPC wrappers and two nested deserializer modules after `2809e6f` cut 0.8.0. `[0.8.0]` already records 117/113/114/112/118; the new call surface had no consumer-facing record. Added `[Unreleased]` covering `create_access_list/2`, `base_fee/1`, `blob_base_fee/1`, `eth_config/1`, and `eth_capabilities/1`.

**2. (fixed) Bare `TODO:` in `mix.exs` for the muex 1.20 scoring bug.**
`8acc2b6` added muex with a comment that already names ROADMAP task 119 but used a bare `TODO:` rather than `TODO(Task 119)`. Relabeled.

**3. (fixed) Mutation campaign scripts `cd` into a retired worktree.**
`.mutation/run.sh` and `.mutation/verify.sh` both `cd /Users/efries/_DATA/worktrees/cartouche/114 || exit 1` — the Task 114 campaign tree, not the repo they live in. Task 119 cannot re-run them as committed. Both now `cd` to the repo root from the script path; `verify.sh` also `mkdir -p`s the gitignored results dir that `run.sh` already created.

## Reviewed clean (no action)

- **Task 117 encode/decode invariant** — V2 constructors lift the bare-address shorthand to `{addr, []}` so encode emits EIP-2930 `[address, []]`; the encode-side access-list guard lives in `TypedDecode.encode_access_list/1` and is shared by V2/V3/V4/V_2930; V3 encode and decode share `valid_blob_versioned_hashes?/1`; V4 encode raises on empty/nil `authorization_list`. Doctest golden bytes were rewritten, not regenerated from the old encoder. Checked against the EIP-2930 access-list shape in this pass.
- **Task 113 vectors/properties** — ethers 6.17.0 and viem 2.55.19 fixtures plus a committed generator; the V1 vector is the EIP-155 worked example (reviewer pin in `6c8acb2`). Properties cover round-trip, recover, chain-domain separation, and low-s via a high-s backend. `docs/verification-ledger.md` names the authorities.
- **Task 114 mutation campaign** — ledger honestly records that muex 0.8.2 cannot produce `:survived` on Elixir 1.20 (Oeditus/muex#20); gap tests and canaries landed; task 119 already owns the re-run. Not re-filed.
- **RPC 120/122/123 implementation** — `defrpc` for the two fee reads; `create_access_list/2` reuses `to_call_params/2` and `JsonField.decode_access_list/1`; EIP-7910 `activationTime`/`blobSchedule` members are JSON numbers and are left as integers (spec: [EIP-7910](https://eips.ethereum.org/EIPS/eip-7910)); `chainId`/`forkId` decode as hex. `Cartouche.Chain` stays a static table and points at `eth_config/1` as an independent read. Unit tests cover omitted/unknown fields; live integration tests pin domain values. Matches the task ACs.
- **No leftover debug** — the only `IO.puts` in `lib/` remains `Cartouche.VM.FFIs.log_ffi/1`.
- **README Status still says `0.2.0` / `~> 0.2` while `mix.exs` is `0.8.0`.** Pre-existing; previous audit (`1c8adb1`) declined to file a rewrite as product copy. Still not filed.

## Reviewer rejections

None recorded for this project.

## Cold check

`mix deps.get && MIX_ENV=test mix check.dispatch` in this un-warmed worktree (no copied `deps` / `_build` / PLTs): **passed** (exit 0; 1246 passed, 38 excluded, 0 failed).
